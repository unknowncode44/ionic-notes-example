import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ws-client2-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
